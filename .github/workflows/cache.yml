name: cache

on: 
    push:
        branches:
            - master

jobs:
    cached:
        runs-on: ubuntu-latest
        steps:
            - name: checkout code
              uses: actions/checkout@v4
        
            - name: cache node
              uses: actions/cache@v4
              with:
                path: ~/.npm